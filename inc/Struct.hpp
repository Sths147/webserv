
#ifndef STRUCT_HPP
# define STRUCT_HPP

// struct DefaultListenServer
// {
// 	Server *_server;
// 	const Listen *_listen;
// 	DefaultListenServer(void) {}
// 	DefaultListenServer(Server *server,const Listen &listen) : _server(server), _listen(&listen) {}
// };

#endif
